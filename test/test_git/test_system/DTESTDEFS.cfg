
# the old style exceptions handling have been removed so these tests
# are obsolete
#SKIPTESTS = test_email_on_exception, test_log_exceptions

[RUN]

    test=dtest-cram --shell=/bin/bash --indent=4 script.t
